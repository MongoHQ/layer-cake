#!/usr/bin/env node

require('coffee-script');
var LayerCake = require('../lib/layer-cake').LayerCake
  , app = new LayerCake(process.cwd());

app.load_plugins();
app.commandline.execute(process.argv);
